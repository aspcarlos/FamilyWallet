<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.FamilyWallet" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Fondo mientras arranca la app -->
        <item name="android:windowBackground">@color/splash_light_green</item>
        <!-- Splash Android 12+ -->
        <item name="android:windowSplashScreenBackground">@color/splash_light_green</item>
    </style>
</resources>